# ADR-0003: Архитектура Filter Engine

- Фильтр реализуется как функция, принимающая Iterable[Row] и возвращающая Iterable[Row].
- Это даёт гибкость в выборе источников данных, поддерживает ленивые вычисления, и позволяет строить пайплайны обработки без лишнего расхода памяти.
